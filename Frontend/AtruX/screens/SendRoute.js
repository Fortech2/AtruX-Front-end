import React from 'react';
import { View, Text, Button } from 'react-native';

const Notification = ({ navigation }) => {
  return (
    <View>
      <Text style = {{top: '150%',}}>Notification</Text>
    </View>
  );
};

export default Notification;
